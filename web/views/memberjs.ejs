<% if (!user) { %>
  window.location = '/login';
<% } else { %>
  var senseBase = {
    user : '<%= user.username %>',
    clientID: '<%= clientID %>' %>,
    homepage : '<%= homepage %>',
    isScraper : <%= user.type == 'Scraper' %>
  }
<% } %>
